name: CSE437-Deploy-Test

on: 
  push:
    branches:
    - main
  pull_request:
    branches:
      - main
jobs:
  build-and-push: 
    runs-on: ubuntu-latest
    steps:
    - name: run linter
      uses: wearerequired/lint-action@v2.3.0
